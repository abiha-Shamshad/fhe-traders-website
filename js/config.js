// ==========================================================================
// FHE Traders — Site configuration
// WHATSAPP_NUMBER is the one value every enquiry button on the site reads.
// Format: country code + number, no "+", no spaces or dashes.
// Email, phone and social links are plain hrefs in the page markup
// (crawlable, and they work without JS), so they are not repeated here.
// ==========================================================================
window.SITE_CONFIG = {
  WHATSAPP_NUMBER: "923202200213",
};
